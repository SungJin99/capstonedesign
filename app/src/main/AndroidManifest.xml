<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CapstoneDesign"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ui.ingredientManagement.IngredientUpdateActivity"
            android:exported="false"
            android:label="식재료 관리"/>
        <activity
            android:name=".ui.community.PostAddActivity"
            android:exported="false"
            android:label="글 작성하기"/>
        <activity
            android:name=".ui.community.PostUpdateActivity"
            android:exported="false"
            android:label="글 수정하기"/>
        <activity
            android:name=".IngredientAddActivity"
            android:exported="false"
            android:label="식재료 추가" />
        <activity
            android:name=".CameraActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".IngredientManagementActivity"
            android:exported="false"
            android:label="@string/title_activity_ingredient_management" />
        <activity
            android:name=".ui.community.PostDetailActivity"
            android:label="게시글 조회" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>